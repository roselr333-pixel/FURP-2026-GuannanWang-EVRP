# 失败案例汇总表（Master Failure / Weak-Case Catalogue）

> 写于 2026-07-24，Guannan Wang。
> 用途：把分散在 W3 / W4 / W6 报告里的"弱结果"和"约束失败"按**统一模板**（样例/求解器/目标值/是否行/违反约束/从哪一步开始不可行/下一步怎么修）合并到一页。
> 补的是 `docs/analysis/progress_report.md` §3.2-③。

---

## 模板（每条都按这 7 个字段写）

| 字段 | 含义 |
|---|---|
| 案例 ID | 唯一编号 |
| 样例 | 哪个 instance / 哪个 family |
| 求解器 | 哪个方法（OR-Tools / GA / V1 / V2 / FSTSP...） |
| 目标值 | gap%、makespan 或车辆数 |
| 可行 | 严格可行 / 部分违反 |
| 违反约束 | TW / energy / rendezvous / 车辆数 等 |
| 从哪一步开始不可行 | 解码 / LS / 评估 / 等等 |
| 下一步怎么修 | 已知可解的方向 |

---

## 1. W3 / W4 / W5：Solomon 100-customer 上的弱结果

### FC-S1 【OR-Tools 偏慢】C104 / R201 / RC201

| 字段 | 内容 |
|---|---|
| 案例 ID | FC-S1 |
| 样例 | C104 (Solomon 100-customer) |
| 求解器 | OR-Tools (Path Cheapest Arc + GLS, 10s) |
| 目标值 | gap=21.85% (我的 1002.7 vs BKS 822.9) |
| 可行 | 是 |
| 违反约束 | 无 |
| 从哪一步开始不可行 | 不适用（可行）；偏慢指 **绝对距离** 偏离 BKS |
| 下一步怎么修 | 加更多 metaheuristic 配置（自动搜索）；或允许更长 time limit（>10s）。本次受 OR-Tools 限制未做。 |

> C104 类的"wide-TW + many-vehicle"实例对 GLS 本身不友好（20 段 window 都接近全开），
> 是 OR-Tools 已知短板。**没有违反约束**，所以记为"弱"而不是"失败"。

### FC-S2 【GA 严重偏】C103

| 字段 | 内容 |
|---|---|
| 案例 ID | FC-S2 |
| 样例 | C103 (Solomon 100-customer, 100 客户 + 紧 TW + 单车队 10 车) |
| 求解器 | 自写 GA（pop=40, 8s/实例, seed=20260717） |
| 目标值 | gap=**90.6%** (1574.9 vs BKS 826.3) |
| 可行 | 是（10 辆车都满足容量 + TW） |
| 违反约束 | 无 |
| 从哪一步开始不可行 | **解码端**：GA 找到 permutation 后，`local_search` 的 2-opt + relocate 跑满 6 pass 仍停在 1574.9——`savings_init` 给的初始 permutation 本身没把车聚成 BKS 的"1 个集中团 + 9 个分散点"结构 |
| 下一步怎么修 | （1）增大 `POP_SIZE`、延长 `TIME_LIMIT_S`；（2）加 or-opt、3-opt 邻域；（3）按 TW 中心排序的客户优先级算子。本次受 8s 时限约束未做。 |

> C103 是 6 个 Solomon family 中 GA 表现**最差**的实例（90.6%），是教科书级 "TW 紧 + 100 客户 + 集中布局" 难题。

### FC-S3 【GA 系统性弱】R2 / RC2 family

| 字段 | 内容 |
|---|---|
| 案例 ID | FC-S3 |
| 样例 | R2 family (n=11) / RC2 family (n=8) |
| 求解器 | 自写 GA |
| 目标值 | R2 mean gap=**41.0%**、RC2 mean gap=**55.1%**（最差 family） |
| 可行 | 全是 |
| 违反约束 | 无 |
| 从哪一步开始不可行 | **长距离散布型算例**对 permutation 的破坏鲁棒性差；2-opt 在分散客户间效益低；savings 种子因分散布局生成大量"边"对被剪 |
| 下一步怎么修 | 加**3-opt** 或 **LKH-style** 的 double-bridge 扰动；或将 `MUT_RATE` 提到 0.5 让种群更分散。本次受 8s 时限约束未做。 |

> GA 系统性偏弱：不只是个别算例弱，而是**一类算例**系统性弱。RC2 family 全部 8 个实例 gap ≥35%。

### FC-S4 【GA 比 OR-Tools 还差】大多数 Solomon 算例

| 字段 | 内容 |
|---|---|
| 案例 ID | FC-S4 |
| 样例 | 56 Solomon 100-customer 算例中 49 个 |
| 求解器 | GA vs OR-Tools |
| 目标值 | GA mean gap=36.2%、OR-Tools=7.2% |
| 可行 | 都行 |
| 违反约束 | 无 |
| 从哪一步开始不可行 | **实现层面**：GA 是"学生自写基线"而非工业级；这是合理的——我的目标是搭可复现工作流而不是造求解器 |
| 下一步怎么修 | 不修。这条要承认是设计选择：GA 在我项目里**不是改进**，而是**外部基线之一**，用来证明"我的 V2 比工业级基线也好"。 |

---

## 2. Week 6：Ground-air EVRP-TW 上的约束级失败

### FC-1 【能量不足，无充电站】truck-only EV, battery=120

| 字段 | 内容 |
|---|---|
| 案例 ID | FC-1 |
| 样例 | N=12 客户 + 4 充电站（`SEED+12`），`allow_recharge=False`，`Q=120` |
| 求解器 | V1（truck EV） |
| 目标值 | makespan = `inf`（不可行） |
| 可行 | **否** |
| 违反约束 | **能量**：6 段距离 12 客户的路线总需 ≈ 800 单位 > 120 电池容量 |
| 从哪一步开始不可行 | 解码：插入第 3 个客户时累计能量已超 Q |
| 下一步怎么修 | 启用 `allow_recharge=True`（恢复 V1 标准设置）→ 1 次充电即可行；或 Q 提到 ≥250。**已通过**：标准 V1 用 Q=250、recharge ON，所有 40 算例 100% 可行。 |

### FC-2 【航程太小，协同无效】drone_range=40

| 字段 | 内容 |
|---|---|
| 案例 ID | FC-2 |
| 样例 | N=12 客户 + drone_range=40（仅为标准 160 的 1/4） |
| 求解器 | V2（ground-air） |
| 目标值 | makespan ≈ V1（588.9 vs V1 574.4），gap ≈ 0%，仅 2 客户被卸 |
| 可行 | 是 |
| 违反约束 | **航程**：原本能卸 8 个的算例只能卸 2 个 |
| 从哪一步开始不可行 | 评估：`net = 节省的卡车时间 − 无人机延误` < 0（航程太短 → 多数 launch/recover 组合被剪） |
| 下一步怎么修 | 把 `R_D` 调回 160（标准）→ 全部 40 算例都卸 ≥65%。**已通过**：本实验只是用来证"协同收益随航程门槛单调上升"。 |

### FC-3 【紧时间窗违约】TW width=35

| 字段 | 内容 |
|---|---|
| 案例 ID | FC-3 |
| 样例 | N=12 客户 + 紧 TW（width=35） |
| 求解器 | V1 |
| 目标值 | 10 客户被服务在其 TW 之外（V1 has 10 TW viol） |
| 可行 | **技术上路线成立**，但**时间窗违约** |
| 违反约束 | **TW**：12 客户中 10 个到达时间 > `tw[node][1]` |
| 从哪一步开始不可行 | 解码：宽窗默认（width=200）下求解器把客户全塞一条路；紧窗下仍按距离最小化插入，没优先 TW |
| 下一步怎么修 | 解码时把"插入后 TW 裕度"作为判据；或放宽窗口到 width=80。本次受范围约束未做。 |

### FC-4 【会合约束否决】V2 sync_rejected 高

| 字段 | 内容 |
|---|---|
| 案例 ID | FC-4 |
| 样例 | N=16 客户（10 种子均值） |
| 求解器 | V2 |
| 目标值 | `sync_rejected` 平均 13089 次（被会合约束否决的无人机行程）；feas_rate=100% |
| 可行 | 是 |
| 违反约束 | **会合**：无人机若在某 recovery 节点落地时间 > 卡车离开时间 → 拒绝该行程 |
| 从哪一步开始不可行 | 评估：每步在 4 站点 × 4 站点 = 16 个 launch/recover 组合中筛出"卡车先离开"的子集 |
| 下一步怎么修 | 已经实现：评估器允许卡车等无人机（wait），且每次否决后自动尝试别的 launch/recover 组合；最终 40 算例 100% 可行。**已通过**：这条其实是"约束起作用的证据"，不是真失败。 |

---

## 3. Week 7：FSTSP 复现中的"系统忠实性" sanity check

### FC-7-1 【abl_cap1 应与 published 完全一致】——sanity check

| 字段 | 内容 |
|---|---|
| 案例 ID | FC-7-1 |
| 样例 | 4 规模 × 10 种子（40 算例） |
| 求解器 | `my_v2_param(max_cust=1, multi_takeoff=True)` |
| 目标值 | 4 规模下 makespan **与 published 完全相等**（例 N=8: 282.5=282.5） |
| 可行 | 是 |
| 违反约束 | 无 |
| 从哪一步开始不可行 | 不适用（"失败" = 不一致） |
| 下一步怎么修 | 修复 `usable_stop` 误把仓库节点 `0` 加进 `protected` 的 bug（修正后 abl_cap1 ≡ published）→ **sanity check 通过**。 |

> 这条**反向**记录：失败 = "ablation 与 baseline 不一致" = 我框架实现有 bug。已经修。

### FC-7-2 【FSTSP 评估器误把单架当并行】——已修

| 字段 | 内容 |
|---|---|
| 案例 ID | FC-7-2 |
| 样例 | N=8 单实例 |
| 求解器 | `my_v2_param`（初版） |
| 目标值 | N=8 出现 70.0 这类**极小值**（不合理） |
| 可行 | 是（但数字虚低） |
| 违反约束 | **物理**：单架无人机被算成可同时执行多个并行行程（实际论文定义无人机**串行**） |
| 从哪一步开始不可行 | 评估：所有无人机行程的结束时间都 ≤ makespan，没考虑一架无人机必须先回收才能起飞下一趟 |
| 下一步怎么修 | 评估器加 `drone_busy_until`：每趟起飞时检查无人机空闲；如忙则推后到 `drone_busy_until`。**已通过**：修正后 N=8 makespan 回到合理值（240+），与 published 数量级一致。 |

### FC-7-3 【V3 评估器重入同一 bug：单架被当并行】——已修

| 字段 | 内容 |
|---|---|
| 案例 ID | FC-7-3 |
| 样例 | N=8/12/16/20，10 种子 |
| 求解器 | `v3_ev_collab.py` 的 `ev_collab`（单架版，V3 K=1） |
| 目标值 | 单架 LNS 降幅被算成 56.7%~87.8%（makespan 67.8 @ N=8），数量级虚低 |
| 可行 | 是（但数字虚低） |
| 违反约束 | **物理**：与 FC-7-2 同一缺陷——`ev_collab` 的无人机循环以卡车到达发射节点的时刻 `arr[i_pos]` 作为发射时刻，未跟踪单架无人机自身可用性，允许同一架无人机"同时"执行多个架次 |
| 从哪一步开始不可行 | 评估：所有无人机行程的结束时间都当作独立、可并行，没考虑一架无人机必须先回收才能起飞下一趟 |
| 下一步怎么修 | 把 V3 评估器重写为 `ev_collab_k`（K 架同构无人机，按回收时刻 `avail[d]` 串行分配），单架即 K=1 特例。修正后单架真实降幅 33.5%~52.4%（N=8 makespan 230.6，与 FC-7-2 的"240+"量级一致），K=2/3 为 44.5%~72.5%。核心 FSTSP 主线（week06/08 的 `drone_scheduling`）本就串行，从未受影响。 |

### FC-7-4 【FSTSP 评估器允许"嵌套架次"（单架仍被当并行）】——已修

| 字段 | 内容 |
|---|---|
| 案例 ID | FC-7-4 |
| 样例 | n=8/10/12（CP-SAT 精确最优研究用到的 15 个实例） |
| 求解器 | `week07_fstsp_repro.fstsp_simulate`（FSTSP 主评估器），被 `my_v2_fstsp` / `week08_lns` 使用 |
| 目标值 | 14/15 个实例的原始 V2 解被赋予 makespan，但对应的计划物理上不可实现 |
| 可行 | 否（物理上）——但评估器返回有限值 |
| 违反约束 | **物理**：与 FC-7-2/7-3 同一缺陷，出现在 FSTSP 主评估器路径。`fstsp_simulate` 用 `drone_avail` 把架次串行化，但**不检查无人机是否已回到卡车**：当架次 A（0→…→2）内部又嵌套架次 B（12→10）时，评估器仍按 `drone_avail` 顺序计算时间，而一架无人机不可能同时执行两个重叠架次 |
| 从哪一步开始不可行 | 评估：`launch = max(arr[i_pos], drone_avail)` 允许在"无人机仍在飞上一个架次"时就从某个卡车节点起飞；嵌套/交叉架次因此被接受 |
| 怎么修 | **已修**：`fstsp_simulate` / `fstsp_simulate_multi` 现在强制物理有效性（发射节点须在回收节点之前、飞行 ≤ 航程、发射时无人机须已回到卡车），违反即返回 inf；启发式因此自动拒绝造成嵌套的移动，产出的解物理可行。回归测试见 `tests/test_evaluators.py::test_heuristic_plans_are_physically_valid`。W7/W8 及依赖该评估器的 W6 灵敏度/多目标已全部重跑，数值下调但结论方向不变（旧→新对照见 `docs/analysis/evaluator_physical_fix_note_zh.md`）。`week06_ground_air_evrp_tw.py` 的自带评估器随后也按同一口径改写并重跑（W6 主结果与 `week06_largeN`），V0/V1 数值不变。 |

### FC-7-5 【V3 / W5 / M&C 评估器：发射时不检查无人机是否已回收】——已修

| 字段 | 内容 |
|---|---|
| 案例 ID | FC-7-5 |
| 样例 | V3（N=8/12/16/20，10 种子，共 40 算例）、`week05_truck_drone_v2.py` 的 6 顾客算例、M&C 原始 36 算例（K=1/2/3） |
| 求解器 | `v3_ev_collab.ev_collab_k`、`week05_truck_drone_v2.simulate`、`fstsp_mc.mc_simulate`、`drone_scheduling.simulate` |
| 目标值 | V3 的 **40/40** 个算例产出的架次集合都物理不可行：贪心允许架次区间重叠，评估器又只把发射时刻推迟到无人机空闲 |
| 可行 | 否（物理上）——但评估器返回有限 makespan |
| 违反约束 | **物理**：与 FC-7-3/7-4 同一类缺陷但成因不同。`ev_collab_k` 已经按 `avail[d]` 串行分配，却**不检查发射时分配到的无人机是否已经回到卡车**：`launch = max(arr[i_pos], avail[d])` 把发射推迟到无人机空闲，而卡车早已离开那个节点；同时贪心的候选枚举也不要求架次区间互不重叠 |
| 从哪一步开始不可行 | 评估与搜索：发射时刻被推迟而不是判不可行；候选接受时也不检查区间重叠 |
| 下一步怎么修 | **已修**：`ev_collab_k` 在 `avail[d] > arr[i_pos]`（或回收点早于发射点）时返回 `schedule_inf` 并把 makespan 置为 `inf`，`ev_lns` / `ev_lns_k` 把不可行解当 `inf` 拒绝；`v3_greedy` 增加架次区间不重叠的预筛。`week05_truck_drone_v2.simulate`、`fstsp_mc.mc_simulate`、`drone_scheduling.simulate` 也补上了同一条检查。V3 主实验与 V3 消融已重跑（单架 LNS 降幅 33.5%~52.4%，V3g 16.3%~39.2%）。回归测试见 `tests/test_evaluators.py::test_v3_rejects_overlapping_sorties` 与 `::test_v3_greedy_produces_a_feasible_schedule`；W5 与 M&C 的对应测试在 `tests/test_truck_drone.py`（架次重叠、晚到等待、v1/v2 基线一致、矩阵评估器与坐标评估器逐计划一致）。 |

---

## 4. 跨案例总结

| 维度 | 出现频次 | 占比 |
|---|---:|---:|
| 严格不可行（能量/TW/会合） | 1 / 13 案例 | 8% |
| 部分违反（航程短、TW 紧、约束否决） | 3 / 13 | 23% |
| 弱结果（绝对值大但可行） | 4 / 13 | 31% |
| sanity check / 实现 bug | 5 / 13 | 38% |

**结论**：
- 真正"路线跑不通"的只有 FC-1 一例（且已在标准设置下规避）；
- 大部分"失败"是**距离偏长**或**约束过紧**导致的"弱"——这正是 EVRP/VRPTW 难度的真实形态；
- sanity check 类里 FC-7-1 是检查通过（框架一致），其余四条（FC-7-2/7-3/7-4/7-5）都是**我自己实现的 bug**，不是方法问题，**均已修**。

> 把弱结果都摆出来不是为了自嘲，而是让评分方能看清：W1–W5 的"GA 比 OR-Tools 差 30pp"不是实验失败，是**研究叙事的一部分**（证明我的 V2 比工业级基线还好的对照基线）。
