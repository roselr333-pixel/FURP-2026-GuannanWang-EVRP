# Pinned dependencies for the FURP EVRP project (reproducibility)
# Install with: pip install -r requirements.txt
# Environment: Python 3.13 venv, Windows
# Note: the PyVRP baseline (baseline_pyvrp_vrptw.py) needs pyvrp; the rest need ortools.

ortools==9.15.6755
pyvrp==0.14.0
vrplib>=1.0        # reads the official Solomon .vrp files (GA and OR-Tools baselines)
numpy>=1.26
pandas>=2.0
matplotlib>=3.7

# Regression tests (tests/)
pytest>=8.0
